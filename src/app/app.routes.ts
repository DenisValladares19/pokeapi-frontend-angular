import { Routes } from '@angular/router';
import { CreateProfileComponent } from './pages/create-profile/create-profile.component';

export const routes: Routes = [{ path: '', component: CreateProfileComponent }];
